public class PackageLevelScope {
    // When a class is declared as public than it is accessible inside the package
    // as well as outside the package. So, it is under the packageLevelScope.
    public void run() {
        System.out.println("Public class");
    }
}
